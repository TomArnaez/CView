export type ChartData<T> = T[];

export type HistogramData = number[];

export type HistogramBin = {
    bin: number;
    count: number;
}

export type LineProfileData = {
    idx: number;
    value: number;
}

