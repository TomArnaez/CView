import { Button } from "@mantine/core"

const Test = () => {
    return     <Button classNames = {{
        root: classes.root
      }}>
        Test
        </Button>
}
import classes from "./css/Button.module.css"

export default Test;